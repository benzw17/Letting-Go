<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lettinggo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/letting_go_icon_xxxhdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/letting_go_icon_xxxhdpi"
        android:supportsRtl="true"
        android:theme="@style/Theme.LettingGo">
        <activity android:name=".LetGoActivity2"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>